<li>
    <p>
        <%= comment.content %>
        <% if(locals.user && locals.user.id == comment.user.id) { %>
            <small>
                <a href="/comments/destroy/<%= comment.id %>">X</a>
            </small>
        <% } %>
        <br>
        <small>
            - <%= comment.user.name %>
        </small>

        <!-- CHANGE :: display the likes of this comment, if the user is logged in, then show the link to toggle likes, else, just show the count -->
        <!-- study about the data attribute -->
        <small>
            <% if(locals.user) { %>
                <a data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%= comment._id %>&type=Comment" class="toggle-like-button">
                    <%= comment.likes.length %> Likes
                </a>
            <% } else { %>
                <%= comment.likes.length %> Likes
            <% } %>
        </small>
    </p>
</li>